'use client'
import userstyles from "@/styles/usersPage.module.scss"

type AppProps = {}

const AppAndSystem: React.FC<AppProps> = (props) => {
    return (
        <section className={`${userstyles["user-info"]}`}>
        </section>
    );
};

export default AppAndSystem;